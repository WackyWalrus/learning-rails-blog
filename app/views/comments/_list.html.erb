<% @post.comments.reverse.each do |comment| 
	@commenter = User.find(comment.user_id) %>
	<div class="comment">
		<%= comment.content %>
		<div class="credits">
			<%= link_to @commenter.name, @commenter %> | <%= comment.created_at.strftime("%m/%d/%Y %I:%M%p") %>
		</div>
	</div>
<% end %>